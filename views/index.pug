extends layout

block content
  div.jumbotron.my-2.shadow-lg.bg-secondary
    h1.display-4.border-bottom.animate__animated.animate__zoomIn E-Window
    p.lead E-Windowは、電子上で気軽に画像を投稿し、共有できるサイトです。
      br
      | 画像をクリックすると詳細情報を見ることができます。
      br 
      | Googleか、GitHubのアカウントでログインすることで、画像を投稿できます。
  div
    a(href="/pictures/new").btn.btn-info.my-3 画像投稿
  - var hasPicture = pictures.length > 0
  if hasPicture
    table.table-striped.table-hover.animate__animated.animate__fadeInUpBig
      tr
      each picture in pictures
        tr
        td
          div.img-wrap.text-center.my-3
            a(href=`/pictures/${picture.pictureId}`, class="thumnail")
              img.border.border-dark(src=picture.pictureUrl,width="50%" height="auto" )
              div.mask
                p.caption #{picture.pictureTitle}